services:
  ols_postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    ports:
      - 5433:5432
    volumes:
      - ols_postgres:/var/lib/postgresql/data

volumes:
  ols_postgres:
