# Base URL used by frontend API client.
# In dev it should usually stay /api and be served through Vite proxy.
VITE_API_BASE_URL=/api

# Optional: target for Vite dev server proxy.
VITE_DEV_API_PROXY_TARGET=http://localhost:8000

# Optional: auth base URL for login redirect.
VITE_AUTH_BASE_URL=/auth
